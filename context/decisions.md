# Decisions

- Simulated services instead of real infra (compute constraint)
- Python-based system (lightweight)
- Deterministic seeds for reproducibility
- Log realism prioritized over complexity
- Penalize brute force actions